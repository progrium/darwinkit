package pdfkit

import "testing"

func TestPDFKitSimple(t *testing.T) {
	// This test just verifies that the package can be compiled.
	// We use a special test file to avoid the Metal package issues.
}